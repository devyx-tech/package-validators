{
  "name": "nestjs-validators-devyx",
  "version": "1.0.8",
  "description": "custom validators",
  "main": "dist/index",
  "types": "dist/index",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "rimraf dist && tsc",
    "prepublish": "npm run build"
  },
  "keywords": [
    "validators",
    "nestjs"
  ],
  "author": "Devyx",
  "license": "MIT",
  "dependencies": {
    "@nestjs/common": "^8.0.6",
    "class-validator": "^0.13.1",
    "cpf-cnpj-validator": "^1.0.3",
    "postgres": "^1.0.2",
    "rxjs": "^7.3.0",
    "typeorm": "^0.2.37"
  },
  "devDependencies": {
    "@types/node": "^16.6.2",
    "rimraf": "^3.0.2",
    "typescript": "^4.3.5"
  }
}
